{
  "name": "inventario-api",
  "version": "1.0.0",
  "description": "API de inventario con Express y Prisma",
  "main": "src/index.js",
  "scripts": {
    "dev": "nodemon src/index.jSs",
    "start": "node src/index.js",
    "db:migrate": "npx prisma migrate dev --name init",
    "db:generate": "npx prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^5.13.0",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.0",
    "prisma": "^5.13.0"
  }
}